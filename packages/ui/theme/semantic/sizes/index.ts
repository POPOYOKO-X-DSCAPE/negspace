import { defineTokens } from "@pandacss/dev";

export const sizes = defineTokens.sizes({
	md: { value: "16px" },
	loaderSpin: { value: "24px" },
	// Diameter of the fold/pack dot circle on the git-history canvas.
	foldDot: { value: "44px" },
});
